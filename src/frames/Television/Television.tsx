import React from "react";

import {sourceURI} from "../../config/settings";

const Television = () => {
  const [channels, setChannels] = React.useState([]);

  React.useEffect(() => {

  }, []);

  return(
    <div className="television">Television</div>
  );
};

export default Television;
